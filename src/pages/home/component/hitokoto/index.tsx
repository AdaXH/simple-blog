// import { TIME, useInterval } from '@/util';
import { memo } from 'react';
// import { getHitokto } from './service';
// import { HitokotoModel } from '../../types';

export const Hitokoto = memo(
  () => {
    // const { hitokoto, from, from_who, resetTimer } = useInterval<HitokotoModel>(getHitokto, TIME.SEC * 20);
    return <></>;
  },
  () => true,
);
